# Development & CI dependencies.
-r requirements.txt
pytest>=8.0,<9.0
httpx>=0.27,<1.0
ruff>=0.4,<1.0
black>=24.0,<26.0

# Test-fixture generation (PDF fixtures); docx fixtures use python-docx.
fpdf2>=2.7,<3.0

# In-memory SQLite for indexing integration tests (no live Postgres needed).
aiosqlite>=0.20,<1.0
