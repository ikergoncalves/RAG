# Runtime dependencies (pinned to compatible ranges for reproducible builds).
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
SQLAlchemy[asyncio]>=2.0,<3.0
asyncpg>=0.29,<1.0
alembic>=1.13,<2.0
qdrant-client>=1.10,<2.0
redis>=5.0,<6.0
python-multipart>=0.0.9,<0.1

# Ingestion / parsing / chunking
pypdf>=4.2,<6.0
python-docx>=1.1,<2.0
markdown-it-py>=3.0,<4.0
beautifulsoup4>=4.12,<5.0
tiktoken>=0.7,<1.0

# Embeddings & vector indexing
openai>=1.30,<2.0
fastembed>=0.3,<1.0

# LLM generation (Claude) — streaming answers + tool-use citation extraction
anthropic>=0.40,<1.0

# Observability: structured logging, Prometheus metrics, Langfuse tracing.
# Langfuse is pinned to the v2 SDK to match the self-hosted langfuse:2 image
# used in docker-compose (the v3 SDK has a different tracing API).
structlog>=24.1,<26.0
prometheus-client>=0.20,<1.0
langfuse>=2.0,<3.0

# Retrieval re-ranking via Cohere's hosted Rerank API. Replaces the local
# cross-encoder (torch + sentence-transformers), keeping the image small and the
# memory footprint viable on hosts with limited RAM (e.g. free deploy tiers).
cohere>=7.0,<8.0
