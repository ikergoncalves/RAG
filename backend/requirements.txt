# Runtime dependencies (pinned to compatible ranges for reproducible builds).
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
SQLAlchemy[asyncio]>=2.0,<3.0
asyncpg>=0.29,<1.0
alembic>=1.13,<2.0
qdrant-client>=1.10,<2.0
redis>=5.0,<6.0
python-multipart>=0.0.9,<0.1

# Ingestion / parsing / chunking
pypdf>=4.2,<6.0
python-docx>=1.1,<2.0
markdown-it-py>=3.0,<4.0
beautifulsoup4>=4.12,<5.0
tiktoken>=0.7,<1.0

# Embeddings & vector indexing
openai>=1.30,<2.0
fastembed>=0.3,<1.0

# Retrieval re-ranking (cross-encoder).
# Torch is resolved from PyTorch's CPU-only wheel index so we don't pull the
# multi-GB CUDA build, which is useless on this CPU-only Docker image. The
# "+cpu" local-version wheels on this index outrank the plain PyPI wheels for
# the same release, so a flexible spec still lands on a CPU build.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2,<3.0
sentence-transformers>=3.0,<6.0
